<UserControl x:Class="Schism.Views.Themes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"   
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:viewmodels="clr-namespace:Schism.ViewModels"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Width="250" Height="75"
             Background="DarkGray">

    <Border BorderBrush="Black" BorderThickness="1" Padding="5" CornerRadius="1" Margin="10,5,5,5" Grid.RowSpan="2" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Row="0">
                <TextBlock Text= "Theme" FontSize="16" FontWeight="Bold" />
            </Grid>
            <Grid Grid.Row="1">
                <TextBlock Text="Select:" Margin="2,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="14"/>
                <ComboBox Name="Theme" ItemsSource="{Binding TS.AvailableThemes, UpdateSourceTrigger=PropertyChanged}" SelectedValue="{Binding TS.SelectedTheme}" Margin="50,0,5,0" VerticalAlignment="Center" HorizontalAlignment="Left" Width="170" Height="25" FontSize="14" SelectionChanged="Theme_SelectionChanged"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
